[build]
builder = "nixpacks"
buildCommand = "bash build.sh"

[deploy]
startCommand = "gunicorn root_app.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10